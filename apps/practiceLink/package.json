{
  "name": "practiceLink",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --force",
    "build": "vue-tsc --build && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@common/api": "workspace:*",
    "@common/form": "workspace:*"
  }
}
